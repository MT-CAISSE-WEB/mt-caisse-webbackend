const consultationQuery = require('./budget.query');

module.exports = {
    budgetQueries: require('./budget.query'),
    DemandeQueries : require('./demandes.query'),
    // Ajouter ici les autres tables
};
