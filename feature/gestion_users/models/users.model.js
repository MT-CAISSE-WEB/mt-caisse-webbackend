class utilisateur {
    constructor (idutilisateur,codeutilisateur,idsociete,nom,prenom,adresse,telephone,email,login,password,typeentitesite,typeentitedepartement,typeentitesociete,acheteur,createdat,createdby,updatedat,updatedby){
                this.idutilisateur = idutilisateur;
                this.codeutilisateur = codeutilisateur;
                this.idsociete = idsociete;
                this.nom = nom;
                this.prenom = prenom;
                this.adresse = adresse;
                this.telephone = telephone;
                this.email = email;
                this.login = login;
                this.password = password;
                this.typeentitesite = typeentitesite;
                this.typeentitedepartement = typeentitedepartement;
                this.typeentitesociete = typeentitesociete;
                this.acheteur = acheteur;
                this.createdat = createdat;
                this.createdby = createdby;
                this.updatedby = updatedby;
                this.updatedat = updatedat;
    }
}
